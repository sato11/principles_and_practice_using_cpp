{
  "tasks": [
    {
      "type": "shell",
      "label": "cpp build active file",
      "command": "/usr/local/bin/g++",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${workspaceRoot}/a.out"
      ],
      "options": {
        "cwd": "/usr/local/bin"
      }
    }
  ],
  "version": "2.0.0"
}
